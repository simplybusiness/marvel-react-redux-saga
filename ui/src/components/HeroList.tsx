import React from 'react';

const HeroList = ({ heroes }): JSX.Element => {
  // Map heroes to create a hero list
  return <div>This is where your hero list will be!</div>;
};

export default HeroList;
